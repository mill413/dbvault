# 功能待办

- 添加数据库查看功能，提供类似 Navicat 的只读数据浏览能力。（已在 `feat/database-browser` 分支实现；原始提交 `df3db55`、测试分支提交 `f5e7ee3` 仅添加过时待办，后续整理提交历史时需要移除。）

# 测试发现的问题

## 已加载修复，等待用户确认或持续回归

- README 的预构建镜像流程原先直接执行 `docker compose up -d`，全新数据卷会因未迁移而导致 API 启动失败。当前部署组已改为使用会先执行 Alembic 的 `deploy.sh`，等待回归。

## 已通过测试

- 新增或编辑用户时，可选邮箱留空会返回 422。修复已把空字符串规范化为 `null`，用户测试通过。
- 管理表单校验组除本地存储默认路径和恢复页面备份选择两项回归外，其余数据库、Kubernetes、Kubeconfig、S3、定时任务表单校验均通过。
- 首次进入页面时，深色模式按钮状态会自动变化，但页面没有真正应用深色主题。修复已在页面挂载时应用主题，用户测试通过。
- 一次性定时任务原先在数据库提交后因调度器不识别 `ONCE` 而返回失败并留下记录；现已在提交前校验并支持 `ONCE` 触发器，自动化回归通过。
- 禁止并发的定时任务完成后未清空 `jobs.active_backup_task_id`；现已改为条件更新释放槽位，调度执行与并发跳过回归通过。
- PostgreSQL 迁移现会先把 `alembic_version.version_num` 扩展到 128，并容忍历史唯一约束不存在；全新安装、从 0005 保留数据升级、缺失旧约束升级三种场景均通过。
- 登录密码错误的 401 原先被前端误报为“Session expired”；浏览器回归确认现会显示“Invalid username or password”，且不会写入令牌。
- 仪表盘备份趋势原先按 UTC 日期分组；现已按服务器本地时区统计，后端完整回归通过。
- 定时任务现会同步并展示 `next_run_at`，停用时清空；Interval 最短支持 5 秒，页面已提供“立即执行”入口。
- 数据库、存储、备份、备份任务、恢复任务和定时任务 API 均返回创建人，管理页面会显示创建用户名。
- 备份完整性校验会持久保存校验时间、结果、期望 SHA256 和实际 SHA256，页面支持刷新后继续查看。
- 本地存储路径现在可留空并使用 `DBVAULT_LOCAL_STORAGE_ROOT` 默认目录，前端与 API 回归通过。
- 恢复页面选择备份后会正确提示选择目标数据库；未选择目标时前端不会提交恢复请求，浏览器回归通过。
- Docker 集成服务的 MySQL、PostgreSQL、MinIO 端口和凭据均支持 `DBVAULT_IT_*` 覆盖，并在测试前等待健康状态；自定义端口集成测试通过。
- K8s 数据库命令已撤回 `/tmp/dbvault-env-*` 临时文件方案，恢复通过 `env` 传递 `MYSQL_PWD`/`PGPASSWORD`；Pod 标签解析、kubeconfig exec 限制和所有权校验保留。
- `.env.example` 已补充所有变量的用途、默认行为和安全说明；中英文升级文档已更新为生产镜像 Compose 模式。
